# wptp-matrix — Design

**D5 (starter):** matrix product begins as versioned JSON + validator. A static site or org portal can consume `data/matrix.v0.json` later.

**Grade policy** matches Chrysalis MASTER-PROGRAM §7. This repo enforces machine-checkable rules only for **Gold**; human review remains required for Silver/Bronze wording in docs.
